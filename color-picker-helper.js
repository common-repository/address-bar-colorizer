jQuery(document).ready(function($) {
    // console.log('Color picker initialized');
    // Adds color picker to 'address-bar-colorizer-color-field' field
    $('.address-bar-colorizer-color-field').wpColorPicker();
});
